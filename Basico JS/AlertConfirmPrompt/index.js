let num1 = parseInt(prompt('Digite um número: '));
let num2 = parseInt(prompt('Digite outro número: '));

const soma = num1 + num2;
const resultado = soma;

console.log(`O resultado da soma dos dois números é: ${resultado}`);